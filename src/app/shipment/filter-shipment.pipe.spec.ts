import { FilterShipmentPipe } from './filter-shipment.pipe';

describe('FilterShipmentPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterShipmentPipe();
    expect(pipe).toBeTruthy();
  });
});
